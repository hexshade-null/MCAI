bot:
  name: "AI_Jane"
  auth: "offline"
server:
  host: "localhost"
  port: 25565
ai:
  model: "glm-5.3-flash"
  api_key: "${ZAI_API_KEY}"
skin:
  file: "/tmp/testskin.png"
  model: "classic"
  upload_url: "http://localhost:8788/mcai/skin"
  token: "changeme"
voice:
  enabled: true
  server_port: 8787
  token: "changeme"
  asr:
    provider: "mock"
  tts:
    provider: "edge"
    voice: "zh-CN-XiaoxiaoNeural"
    fallback_text: true
probe:
  name: "TestPlayer"
  timeout_ms: 25000
