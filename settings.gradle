rootProject.name = 'mcaibridge'
include 'core', 'gui', 'paper'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url 'https://repo.opencollab.dev/main/' }
        maven { url 'https://repo.papermc.io/repository/maven-public/' }
    }
}
